# Simple App

This is a small browser-based to-do application built with plain HTML, CSS, and JavaScript.

## Run it locally

Open `index.html` in a browser, or run a local web server:

```bash
python -m http.server 8000
```

Then visit `http://localhost:8000`.
